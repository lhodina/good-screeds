extends layout.pug
include utils.pug
block content
    link(rel='stylesheet' href='/styles/form.css')
    +validationErrorSummary(errors)
    div(class="form-container")
        form(class="screed-form" action=`/screeds/${screed.id}/edit` method='post')
            include screed-form-fields.pug
            br
            button(type='submit') Update
            a(href='/') Cancel
