extends layout.pug
include utils.pug
block content
    +validationErrorSummary(errors)
    form(action='/shelves' method='post')
        include shelf-form-fields.pug
        br
        button(type='submit') Add Shelf
        a(href='/') Cancel
